<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    android:paddingTop="?android:attr/actionBarSize"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <CheckBoxPreference
        android:defaultValue="false"
        android:key="computer_first"
        android:title="@string/computer_first_title"
        />
    <ListPreference
        android:entries="@array/field_size"
        android:entryValues="@array/field_size_values"
        android:key="field_size"
        android:title="@string/field_size_title"
        />
</PreferenceScreen>